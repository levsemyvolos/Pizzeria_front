import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";
import "./App.css"; // базові стилі
import "@fontsource/roboto"; // шрифт Material UI

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<App />);
